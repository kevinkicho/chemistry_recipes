# ── Secrets & environment (never commit) ─────────────────────────
.env
.env.local
.env.development
.env.development.local
.env.test
.env.test.local
.env.production
.env.production.local
.env*.local
web/.env
web/.env.local
web/.env*.local
!.env.example
!web/.env.example

# Private keys & certificates
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
id_dsa
id_ecdsa
*.secret
.secrets/

# Secrets directory: track README only; ignore all credential files
/secrets/**
!/secrets/README.md

# Credential / service-account dumps
credentials.json
*-credentials.json
service-account*.json
google-services.json
auth.json

# Firebase Admin SDK private keys (never commit) — including accidental root copies
*firebase-adminsdk*.json
*-firebase-adminsdk-*.json
firebase-adminsdk*.json
**/serviceAccountKey.json
.firebase/admin/

# Local npm auth (may contain tokens)
.npmrc

# ── Dependencies ─────────────────────────────────────────────────
node_modules/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# ── Next.js / build ──────────────────────────────────────────────
web/.next/
web/out/
web/build/
web/dist/
web/tsconfig.tsbuildinfo
*.tsbuildinfo

# ── Debug / logs ─────────────────────────────────────────────────
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.log
logs/

# ── Agent tooling (local only) ───────────────────────────────────
.agents/
.claude/
skills-lock.json
.grok/

# ── OS / editor ──────────────────────────────────────────────────
.DS_Store
Thumbs.db
Desktop.ini
.idea/
.vscode/*
!.vscode/extensions.json

# ── Test / coverage ──────────────────────────────────────────────
coverage/
.nyc_output/
web/diagnostics-output/

# ── Vercel / deploy ──────────────────────────────────────────────
.vercel

# ── Misc local ───────────────────────────────────────────────────
*.local
.tmp/
tmp/
.cache/
